from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='new_york_city_taxi analysis that predicts the total ride duration ',
    author='rup',
    license='',
)
